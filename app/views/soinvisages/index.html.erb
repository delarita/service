<%# <%= form_for([@bc_soinvisage, @order_item], remote: true) do |f| %>
<%#  <%= f.hidden_field :orderitemable_type, value: "Soinvisage" %>
<%#  <%= f.hidden_field :orderitemable_id, value: @bc_soinvisage.id %>
<%#  <%= f.hidden_field :quantity, value: 1 %>
<%#  <% @order_item = @boncadeau %>
<%#  <% p @order_item %>
<%#  <%= f.submit "BON CADEAU", class: 'btn btn-primary btn-sm' %>
<%# <% end %>


<div class="navmenu-presta">
  <%# <li><%= link_to "ACCUEIL", root_path%>
  <%# </li> %>
  <%# <li><a href="#about">A PROPOS</a></li> %>

  <div class="logo"></div>
  <li><%= link_to "EPILATIONS", epilations_path %></li>
  <li><%= link_to "MASSAGES", massages_path %></li>
  <li><%= link_to "SOINS DU VISAGE", soinvisages_path %></li>
  <li><%= link_to "MAQUILLAGE PERMANENT", maquillages_path %></li>
</div>

<div class="false-menu"></div>

<div class="under-menu">
  <h1 class="under-title">Les Soins du Visages</h1>
</div>

<div class="container">
  <p>Description</p>
  <div class="makeup-container">
    <div class="card-wrapper">
      <% @soinvisages.each do |soinvisage| %>
        <div class="cards">
          <div class="service">
            <%= form_for([soinvisage, @order_item], remote: true) do |f| %>
              <%= f.hidden_field :orderitemable_type, value: "Soinvisage" %>
              <%= f.hidden_field :orderitemable_id, value: soinvisage.id %>
              <%= soinvisage.name %><%= " " if soinvisage.description != "" %>
              <%= soinvisage.description %><%= "~" if soinvisage.price != "" %>

              <div class="form-group">
                <%= f.number_field :quantity, value: 1, min: 1, class: 'form-control' %>
              </div>

              <div class="service-price">
                <span class="price">
                 <%= humanized_money_with_symbol(soinvisage.price) %>
                </span>
                <%= f.submit "Offrir", class: 'btn btn-ghost2 btn-sm' %>
              </div>
            <% end %>
            <%= link_to "En savoir plus", soinvisage_path(soinvisage) %>
            <% if policy(Soinvisage).destroy? %>
              <p>
                <%= link_to "del",
                  soinvisage_path(soinvisage),
                  method: :delete,
                  data: { confirm: "Are you sure?" } %>
              </p>
            <% end %>
          </div>
        </div>
      <% end %>
    </div>
  </div>
  <% if policy(Soinvisage).new? %>
    <p>
      <%= link_to "Nouvelle Prestation", new_soinvisage_path %>
    </p>
  <% end %>
</div>


